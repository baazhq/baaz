apiVersion: v1
kind: Service
metadata:
  name: {{ include "ballasdata.fullname" . }}
  labels:
    {{- include "ballasdata.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ballasdata.selectorLabels" . | nindent 4 }}
